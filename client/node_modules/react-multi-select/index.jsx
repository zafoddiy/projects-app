'use strict';

var React       = require('react')
var MultiSelect = require('./src')

var App = React.createClass({

    render: function(){

        var onChange = function(value){
            VALUE = value
            this.setState({})
        }.bind(this)

        return <div style={{position: 'relative', width: '100%', height: '100%'}}>
                <MultiSelect style={{width: 370, padding: 2, margin: 50}}
                    onChange={onChange}
                    />
            </div>
    }
})

React.render((
    <App />
), document.getElementById('content'))